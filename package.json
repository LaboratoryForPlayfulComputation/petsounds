{
  "name": "PetSounds",
  "version": "1.0.0",
  "description": "A device for analyzing live and recorded high-frequency audio",
  "dependencies": {
    "express": "^4.10.2",
    "mic": "^2.1.2",
    "node-arecord": "^0.0.2",
    "node-sass": "^5.0.0",
    "node-wav": "^0.0.2",
    "socket.io": "^1.7.2",
    "socket.io-stream": "^0.9.1",
    "wav-decoder": "^1.3.0",
    "websocket-stream": "^5.5.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "node-sass ./public/scss -o ./public/css",
    "start": "node index.js",
    "predev": "node-sass ./public/scss -o ./public/css",
    "dev": "nodemon index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
